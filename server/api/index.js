const { callback } = require("callback");
const { authorize } = require("authorize");
const { pushtoken } = require("pushtoken");

module.exports = {
	callback: callback,
	authorize: authorize,
	pushtoken: pushtoken,
};
